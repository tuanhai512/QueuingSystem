import React, { FC } from 'react'
import SideBar from '../SideBar/SideBar'

type Props = {}

export const Role:FC = ({}: Props) => {
  return (
      <>
       <SideBar/>
    <div>Role</div>
      </>
     
  )
}

