import React, { FC } from 'react'
import SideBar from '../SideBar/SideBar'

type Props = {}

export const Report:FC = (props: Props) => {
  return (
      <>
      <SideBar/>
      <div>Report</div>
      </>
   
  )
}