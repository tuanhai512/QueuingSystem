import React, { FC } from 'react'
import SideBar from '../SideBar/SideBar'

type Props = {}

export const User:FC = (props: Props) => {
  return (
      <>
      <SideBar/>
      <div>User</div>
      </>
   
  )
}

