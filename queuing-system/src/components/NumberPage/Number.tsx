import React, { FC } from 'react'
import SideBar from '../SideBar/SideBar'

type Props = {}

export const Number: FC = (props: Props) => {
  return (
      <>
      <SideBar/>
      <div>Number</div>
      </>
   
  )
}