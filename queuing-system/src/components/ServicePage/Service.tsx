import React, { FC } from 'react'
import SideBar from '../SideBar/SideBar'

type Props = {}

export const Service:FC = (props: Props) => {
  return (
      <>
      <SideBar/>
      <div>Service</div>
      </>
   
  )
}